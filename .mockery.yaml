# Mockery configuration for generating mocks
# Documentation: https://vektra.github.io/mockery/latest/configuration/

# Output directory for all mocks
dir: "mocks"

# Package name for mock implementations
mockname: "Mock{{.InterfaceName}}"

# Output file naming pattern
filename: "mock_{{.InterfaceName | snakecase}}.go"

# Generate mocks for all matching interfaces
all: false

# Enable inpackage mode (generate mocks in the same package)
inpackage: false

# Disable version comments in generated files
disable-version-string: false

# Generate with testify expect methods
with-expecter: true

# Package-specific configurations
packages:
  github.com/adalundhe/sylk/agents/archivalist:
    config:
      dir: "agents/archivalist/mocks"
      outpkg: "mocks"
    interfaces:
      # Main service interfaces
      ArchivalistService:
        config:
          mockname: "MockArchivalist"

      StoreService:
        config:
          mockname: "MockStore"

      ArchiveService:
        config:
          mockname: "MockArchive"

      ClientService:
        config:
          mockname: "MockClient"

      RegistryService:
        config:
          mockname: "MockRegistry"

      EventLogService:
        config:
          mockname: "MockEventLog"

      ConflictDetectorService:
        config:
          mockname: "MockConflictDetector"

      QueryCacheService:
        config:
          mockname: "MockQueryCache"

      EmbeddingStoreService:
        config:
          mockname: "MockEmbeddingStore"

      SemanticRetrieverService:
        config:
          mockname: "MockSemanticRetriever"

      SynthesizerService:
        config:
          mockname: "MockSynthesizer"

      MemoryManagerService:
        config:
          mockname: "MockMemoryManager"

      ToolHandlerService:
        config:
          mockname: "MockToolHandler"

      AgentContextService:
        config:
          mockname: "MockAgentContext"

      ConflictHistoryService:
        config:
          mockname: "MockConflictHistory"

      # Embedder interface (already defined in embeddings.go)
      Embedder:
        config:
          mockname: "MockEmbedder"

  github.com/adalundhe/sylk/agents/guide:
    config:
      dir: "agents/guide/mocks"
      outpkg: "mocks"
    interfaces:
      # Core routing interfaces
      RouterService:
        config:
          mockname: "MockRouter"

      ClassifierService:
        config:
          mockname: "MockClassifier"

      PendingStoreService:
        config:
          mockname: "MockPendingStore"

      RouteCacheService:
        config:
          mockname: "MockRouteCache"

      RegistryService:
        config:
          mockname: "MockRegistry"

      # Health and circuit breaker interfaces
      HealthMonitorService:
        config:
          mockname: "MockHealthMonitor"

      CircuitBreakerService:
        config:
          mockname: "MockCircuitBreaker"

      CircuitBreakerRegistryService:
        config:
          mockname: "MockCircuitBreakerRegistry"

      DeadLetterQueueService:
        config:
          mockname: "MockDeadLetterQueue"

      # DSL and retry interfaces
      ParserService:
        config:
          mockname: "MockParser"

      RetryPolicyService:
        config:
          mockname: "MockRetryPolicy"

      HeartbeatSenderService:
        config:
          mockname: "MockHeartbeatSender"

      # EventBus already defined in bus.go
      EventBus:
        config:
          mockname: "MockEventBus"

      Subscription:
        config:
          mockname: "MockSubscription"

      # Classifier client for LLM mocking
      ClassifierClient:
        config:
          mockname: "MockClassifierClient"

  github.com/adalundhe/sylk/core/providers:
    config:
      dir: "core/providers/mocks"
      outpkg: "mocks"
    interfaces:
      StreamPublisher:
        config:
          mockname: "MockStreamPublisher"

      StreamProvider:
        config:
          mockname: "MockStreamProvider"

      ProviderAdapter:
        config:
          mockname: "MockProviderAdapter"

  github.com/adalundhe/sylk/core/claims:
    config:
      dir: "core/claims/mocks"
      outpkg: "mocks"
    interfaces:
      ValidationDispatcher:
        config:
          mockname: "ValidationDispatcher"

      ValidatorHandler:
        config:
          mockname: "ValidatorHandler"

      AgentTurnEvaluator:
        config:
          mockname: "AgentTurnEvaluator"

      ArtifactLifecycleBusSink:
        config:
          mockname: "ArtifactLifecycleBusSink"

      ValidationLifecycleBusSink:
        config:
          mockname: "ValidationLifecycleBusSink"

      ClaimsClock:
        config:
          mockname: "ClaimsClock"

      ClaimsMetricsSink:
        config:
          mockname: "ClaimsMetricsSink"

      TestamentGenerator:
        config:
          mockname: "TestamentGenerator"

      DeltaPublisher:
        config:
          mockname: "DeltaPublisher"

      DeltaSubscriber:
        config:
          mockname: "DeltaSubscriber"

      DeltaSubscription:
        config:
          mockname: "DeltaSubscription"

      DeltaBus:
        config:
          mockname: "DeltaBus"

      ClaimsProjector:
        config:
          mockname: "ClaimsProjector"

      AgentRefResolver:
        config:
          mockname: "AgentRefResolver"

      ClaimPostPolicy:
        config:
          mockname: "ClaimPostPolicy"

      ScopeProvider:
        config:
          mockname: "ScopeProvider"

      ServiceHandler:
        config:
          mockname: "ServiceHandler"

      PendingContinuationRecoverer:
        config:
          mockname: "PendingContinuationRecoverer"

      ClaimWorkCanceller:
        config:
          mockname: "ClaimWorkCanceller"

      ShutdownScope:
        config:
          mockname: "ShutdownScope"

      ShutdownInbox:
        config:
          mockname: "ShutdownInbox"

      ShutdownContinuationStore:
        config:
          mockname: "ShutdownContinuationStore"

      ExpectedToolExecutor:
        config:
          mockname: "ExpectedToolExecutor"

      ExpectedToolPolicy:
        config:
          mockname: "ExpectedToolPolicy"

      ExpectedToolArgumentRedactor:
        config:
          mockname: "ExpectedToolArgumentRedactor"

      ValidationExpectedToolRemediationPoster:
        config:
          mockname: "ValidationExpectedToolRemediationPoster"

      DAGProcessorBackend:
        config:
          mockname: "DAGProcessorBackend"

      VFSProvisionerBackend:
        config:
          mockname: "VFSProvisionerBackend"

      ToolRuntimeBackend:
        config:
          mockname: "ToolRuntimeBackend"

      KnowledgeBackend:
        config:
          mockname: "KnowledgeBackend"

      MemoryContinuityBackend:
        config:
          mockname: "MemoryContinuityBackend"

      DocumentBackend:
        config:
          mockname: "DocumentBackend"

      GuardianBackend:
        config:
          mockname: "GuardianBackend"

      ProviderGatewayBackend:
        config:
          mockname: "ProviderGatewayBackend"

      ExternalAdapterBackend:
        config:
          mockname: "ExternalAdapterBackend"

  github.com/adalundhe/sylk/core/forest:
    config:
      dir: "core/forest"
      outpkg: "forest"
      inpackage: true
      filename: "mock_{{.InterfaceName | snakecase}}_test.go"
    interfaces:
      NeighborIndex:
        config:
          mockname: "MockNeighborIndex"

  github.com/adalundhe/sylk/ui/bridge:
    config:
      dir: "ui/bridge/mocks"
      outpkg: "mocks"
    interfaces:
      ClaimPresentationSink:
        config:
          mockname: "ClaimPresentationSink"

      ClaimArtifactSink:
        config:
          mockname: "ClaimArtifactSink"

  github.com/adalundhe/sylk/core/pipeline:
    config:
      dir: "core/pipeline/mocks"
      outpkg: "mocks"
    interfaces:
      # PH.6 Handoff Archival interfaces
      BleveStore:
        config:
          mockname: "MockBleveStore"

      VectorStore:
        config:
          mockname: "MockVectorStore"

      ArchivalEmbedder:
        config:
          mockname: "MockArchivalEmbedder"

      ArchivableHandoffState:
        config:
          mockname: "MockArchivableHandoffState"

      # PH.1 Handoff Manager interfaces
      HandoffArchiverService:
        config:
          mockname: "MockHandoffArchiverService"

      AgentFactoryService:
        config:
          mockname: "MockAgentFactoryService"

      AgentService:
        config:
          mockname: "MockAgentService"
