Now to extend upon this - *how could we support displaying multiple files at the same time in a single     
  edit panel*? Consider we can split the view in half vertically and likewise in half horizontally. The    
  user could use alt + | to trigger a vertical split on the edit panel or a subpanel of the edit panel, and
  alt + _ to trigger a horizontal split on the edit panel or a subpanel of the edit panel.    